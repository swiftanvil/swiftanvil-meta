# SwiftAnvil — Resume Here

> Human-facing session resumption guide. For agent startup, see `meta.session-start`.

---

## Where We Left Off

| Item | Value |
|------|-------|
| **Last Session** | 2026-06-06 |
| **Active Phase** | Phase 9 — iStudio Boundary & Tooling Expansion |
| **Roadmap Child State** | 0/2 boundary children complete (`planning.child-9-5`, `planning.child-9-6`) |
| **Blocking Health Gate** | Package PMS below 80 requires an improvement sprint before lower-priority work |
| **Immediate Next Work** | Start PMS improvement sprint with `swiftanvil-anvil-macros` |
| **After PMS Gate Clears** | Finish Child 9.5 review/result/provenance, then start Child 9.6 |

## Quick Context

- **Phases 1–8 are complete.** Phase 9 is active.
- **Platform policy is currently compliant.** `packages.registry` says actively maintained packages meet `policy.platform`.
- **PMS is not healthy yet.** `AnvilMacros` is F, and `AnvilCore`, `AnvilMenuBar`, `AnvilSettings`, and `AnvilWindow` are C.
- **Child 9.5 has partial deliverables.** `BOUNDARY.md`, `AGENTS.md` redirect rules, session-start boundary checks, and registry entries exist, but `Children/9.5/` only has `PLAN.md`. It still needs the workflow artifacts required by `workflow.orchestration`.
- **Child 9.6 depends on 9.5.** Do not start validator migration until Child 9.5 is workflow-complete and the PMS gate is cleared or explicitly overridden.

## What to Do Next

1. **If resuming as agent:** Read `meta.agents` -> `meta.registry` -> `meta.session-start` -> follow the health gate before roadmap work.
2. **If resuming as human:** Check `packages.registry` and `improvement.dashboard` first. The next package sprint starts with `AnvilMacros`: add DocC catalog, add CI workflow, tag release, recalculate PMS.
3. **After package PMS is healthy:** Complete `planning.child-9-5` workflow artifacts (`REVIEW-PLAN.md`, `RESULT.md`, `REVIEW-PROVENANCE.md`) and update roadmap/package memory.
4. **Then:** Start `planning.child-9-6` validator migration.

## Repository Layout (Local)

```
~/Documents/v-i-s-h-a-l/swiftanvil/
├── swiftanvil-meta/          ← START HERE — org memory, roadmap, planning
├── swiftanvil-cli/           ← CLI tool (swiftanvil command)
├── swiftanvil-anvil-runner/  ← Managed worker tooling
├── swiftanvil-enforcement/   ← Shared enforcement scripts
├── .github/                  ← Org profile repo
└── swiftanvil-anvil-*/       ← Individual packages (a11y, bench, strings, etc.)
```

## Important Notes

- `swiftanvil-meta/` is the **single source of truth** for organization planning.
- The old `iFoundation` repo (in `~/Documents/v-i-s-h-a-l/github/iFoundation/`) is **archived/historical**.
- All phase children now live in `swiftanvil-meta/Children/`.
- The orchestration framework requires **cross-host review** (different model from builder) for plan and implementation reviews.
- `ORG_REPORT.md` is generated by `script.org-report`; regenerate it after completing the PMS sprint or Child 9.5.

---

*Last updated: 2026-06-06*
