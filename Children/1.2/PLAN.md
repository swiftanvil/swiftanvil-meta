# Child 1.2 Plan: Extract A11yIdentifiers from Turnip iOS

## Objective
Extract the A11yIdentifiers package from Turnip iOS into a standalone, publishable Swift package.

## Goals
- [ ] Extract with git history preserved
- [ ] Generalize (remove Turnip-specific code)
- [ ] Add comprehensive tests
- [ ] Add documentation (README, DocC)
- [ ] Verify it builds and tests pass independently

## Non-Goals
- [ ] Don't publish to GitHub yet (wait for org setup)
- [ ] Don't add new features beyond generalization
- [ ] Don't change core API design (keep Turnip's pattern)

## Source Location
`/Users/vishalsingh/Documents/Turnip.gg/turnip-ios/zaps-app/A11yIdentifiers/`

## Task Breakdown

| # | Task | Effort | Verification |
|---|------|--------|--------------|
| 1 | Verify source exists and is complete | 5 min | `ls` shows expected files |
| 2 | Extract with git subtree split | 10 min | New branch has history |
| 3 | Review all source files for Turnip refs | 15 min | `grep -r "Turnip"` returns nothing |
| 4 | Generalize: remove hardcoded module enums | 20 min | Only core types remain |
| 5 | Add unit tests for core types | 15 min | ≥80% coverage |
| 6 | Write README with usage examples | 10 min | Clear, complete |
| 7 | Write Package.swift | 5 min | Valid, minimal |
| 8 | Verify build | 5 min | `swift build` passes |
| 9 | Verify tests | 5 min | `swift test` passes |

## Generalization Details

### What Stays
- `A11yID` struct (RawRepresentable, ExpressibleByStringLiteral)
- SwiftUI `.a11yID()` extension
- UIKit `.setA11ID()` extension
- `appending()` methods

### What Changes
- Remove all `SettingsA11y`, `HomeA11y`, etc. enums
- Keep one example enum in README/docs
- Module name stays `A11yIdentifiers`

### What Gets Generated Later (by CLI)
```swift
// This will be generated by ifoundation-cli per project:
public enum SettingsA11y {
    public enum AccountSection {
        public static let logoutRow: A11yID = "settings.account.logout"
    }
}
```

## Risks

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| Turnip code has hidden dependencies | Medium | Thorough grep + build test |
| Git history extraction fails | Low | Use git subtree split carefully |
| Swift version mismatch | Low | Update Package.swift platform |

## Success Criteria
- Package builds with `swift build` ✅
- Tests pass with `swift test` ✅
- No Turnip-specific references ✅
- README explains standalone usage ✅
- Can be imported by any Swift project ✅

## Output Location
`Packages/ifoundation-a11yidentifiers/`

## Dependencies
- Child 1.1 research (for patterns)
- Access to Turnip iOS repo
